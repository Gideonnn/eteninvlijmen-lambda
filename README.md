# Eten in Vlijmen API on AWS Lambda

## Usage

```
# Install serverless
$ npm i -g serverless

# Set credentials for AWS
$ serverless config --key YOUR-ACCESS-KEY-ID --secret YOUR-SECRET-ACCESS-KEY

# Deploy!
$ serverless deploy
```
